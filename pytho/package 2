#use reduce to compute sum of elements in list#
from functools import reduce

# list of numbers
numbers = [1, 2, 3, 4, 5]

#using reduce to calcuate sum
total  = reduce(lambda x, y: x + y, numbers)

print("sum of elements :",total)